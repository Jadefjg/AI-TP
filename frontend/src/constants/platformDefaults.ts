/** Default SUT / API base URL for local dev (matches vite proxy → backend :8002). */
export const DEFAULT_BASE_URL = "http://127.0.0.1:8002";

export const DEFAULT_HEALTH_URL = `${DEFAULT_BASE_URL}/system/health`;
